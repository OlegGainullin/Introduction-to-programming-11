#include <stdio.h>
#include <locale.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    char *locale = setlocale(LC_ALL, "");
    int x = 0;
    int y = 0;
    int c = 0;
    int answer = 0;
    printf("Введите число: ");
    scanf("%d", &x);
        if (x % 2 == 0)
            printf("четное ");
        else
            printf("нечетное ");
        if (x > 0 && x < 10)
            printf("однозначное ");
        if (x > 9 && x < 100)
            printf("двузначное ");
        if (x > 99 && x < 1000)
            printf("трехзначное ");
    printf("число \n");
    return 0;
}
